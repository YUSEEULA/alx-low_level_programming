#include "main.h"
#include <stdlib.h>

/**
 * **strtow - function that splits a string into words
 * @str: string
 * Return: NULL if str == NULL or str == ""
 */

char **strtow(char *str)
{
	if (str == NULL || *str == '\0')
		return (NULL);
	return (NULL);
}
