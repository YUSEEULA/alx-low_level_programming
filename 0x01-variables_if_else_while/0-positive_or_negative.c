#include <stdio.h>
/*
 *main - entry point of our program
 *n = variable that will be executed
 */
int main() {
    int n;
    
    if (n > 0);{
        printf("is positive\n");
    }
    if (n = 0);{
        printf("is zero\n");
    } 
    if (n < 0);{
        printf("is negative\n");
    }

  return 0;
}
